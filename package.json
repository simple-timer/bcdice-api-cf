{
	"name": "bcdice-api-cf",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"build": "esbuild src/index.ts --bundle --format=esm --platform=browser --target=es2022 --conditions=workerd,worker,browser --minify --legal-comments=none --outfile=dist/index.js && terser dist/index.js --compress passes=3 --mangle --output dist/index.js",
		"deploy": "wrangler deploy",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"postinstall": "bun scripts/remove-maps.ts && bun scripts/patch-bcdice.ts && bun scripts/generate-i18n-loader.ts",
		"check": "bunx biome check",
		"compat": "bun scripts/verify_updates.ts"
	},
	"dependencies": {
		"@hono/valibot-validator": "^0.6.1",
		"bcdice": "^4.10.0",
		"hono": "^4.13.0",
		"valibot": "^1.4.2"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.14",
		"@socketsecurity/bun-security-scanner": "^1.1.2",
		"@types/bun": "^1.3.14",
		"esbuild": "^0.28.1",
		"terser": "^5.49.0",
		"typescript": "^6.0.3",
		"wrangler": "^4.119.0"
	}
}
